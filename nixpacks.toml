providers = ["python"]

[python]
runtime = "3.12"

[build]
command = "cd backend && pip install -r requirements.txt && python -m app.scripts.seed_matrix"

[deploy]
command = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
